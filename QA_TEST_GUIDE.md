# GyeongdoPlus 종합 테스트 가이드 (QA 매뉴얼)

본 문서는 GyeongdoPlus 프로젝트의 모든 기능이 기획 의도대로 정상 작동하는지 검증하기 위한 테스트 시나리오를 담고 있습니다.

## 1. 테스트 준비 사항
*   **기기 연동**: 최소 2대 이상의 실물 안드로이드/iOS 기기 (경찰 vs 도둑 테스트용)
*   **웨어러블**: WearOS 또는 watchOS 기반 스마트워치 (심박수 측정 및 핑 테스트용)
*   **권한 설정**: 위치 서비스(GPS), 블루투스(BLE), 알림 권한 허정
*   **네트워크**: 서버 연결이 가능한 인터넷 환경

## 2. 주요 테스트 시나리오

### A. 인증 및 보안 (Auth)
1.  **소셜 로그인 연동**: 카카오/구글 로그인이 실제 API와 연동되어 사용자 정보를 정상적으로 가져오는지 확인합니다.
2.  **릴리스 빌드 보안**: 릴리스(Release) 빌드에서 테스트 로그인(Stub/Mock) 버튼이 비활성화되거나 동작하지 않는지 확인합니다.

### B. 방 입장 및 설정 (Room)
1.  **방 생성 및 입장**: 방 코드를 통해 서버 기반 방에 정상적으로 입장되는지 확인합니다.
2.  **실시간 설정 동기화**: 방장(Host)이 게임 시간 제한을 변경했을 때, 소켓(Socket.IO)을 통해 위젯과 모든 참가자에게 즉시 반영되는지 확인합니다.
3.  **오프라인 코드 차단**: `'0000'`, `'TEST'` 등 하드코딩된 테스트용 코드로 입장이 불가능해졌는지 확인합니다.

### C. 게임 플레이 코어 (In-Game)
1.  **GPS 위치 추적**: `Geolocator`를 통해 실제 내 위치가 서버로 주기적으로 전송되는지 확인합니다.
2.  **Telemetry 데이터**: 심박수(워치 연동 시), 헤딩(Heading), 속도 정보가 포함된 텔레메트리 샘플이 정상적으로 구성되는지 확인합니다.
3.  **권한 예외 처리**: 게임 시작 시 위치 권한이 거부된 경우, 적절한 경고 메시지와 함께 시스템 설정 이동을 안내하는지 확인합니다.

### D. 근접 감지 및 체포 (Proximity/Arrest)
1.  **근접 진동 반응**: 경찰이 도둑에게 접근할 때 거리(5m -> 3m -> 1.5m)에 따라 햅틱(Haptic) 진동 강도가 점진적으로 강해지는지 확인합니다.
2.  **자동 체포 로직**: GPS 상 거리가 3m 이내로 일정 시간 유지될 때 자동 체포 요청이 서버로 전송되는지 확인합니다.
3.  **BLE 광고 제한**: 현재 기술적 한계로 BLE Advertising은 비활성화 상태이며, GPS 기반 로직으로 정상 폴백되는지 확인합니다.

### E. 워치 연동 기능 (Watch)
1.  **실시간 심박수**: 워치에서 측정된 심박수 데이터가 폰의 텔레메트리 데이터에 실시간으로 포함되는지 확인합니다.
2.  **워치 핑(Ping)**: 워치에서 핑 버튼 클릭 시 폰에서 진동이 발생하고 서버로 `watch_ping` 이벤트가 전송되는지 확인합니다.

### F. 전적 및 결과 (Records)
1.  **게임 결과 저장**: 게임 종료 후 승패 결과가 서버 DB에 정상 기록되고, 전적 탭에서 Match ID, 역할, 결과가 올바르게 표시되는지 확인합니다.
2.  **데이터 매핑 제한**: 현재 백엔드 DTO 제약으로 인해 `mode`, `ratingDelta`, `distanceM`은 기본값으로 표시되며, 추후 서버 확장이 필요함을 인지합니다.

## 3. 기술적 제약 및 잔여 과제
*   **BLE Advertising**: `flutter_blue_plus` 라이브러리의 한계로 인해 네이티브 플랫폼 채널(Java/Kotlin, Swift) 구현을 통한 보완이 필요합니다.
*   **백엔드 DTO 확장**: 전적 상세 데이터(Rating 변동 등)를 위해 백엔드 API의 반환 규격 추가가 필요합니다.
