import 'package:flutter/material.dart';

class AppColors {
  // Backgrounds
  static const bgTop = Color(0xFF0B1224);
  static const bgBottom = Color(0xFF07101F);

  // Surfaces
  static const surface = Color(0xFF101C33);
  static const surface2 = Color(0xFF0E1830);

  // Text
  static const textPrimary = Color(0xFFEAF2FF);
  static const textSecondary = Color(0xFF8FA2C7);

  // Accents
  static const ally = Color(0xFF22D3EE);   // cyan
  static const enemy = Color(0xFFFF4D5A);  // red
  static const safe = Color(0xFF35D07F);   // green
  static const warn = Color(0xFFFFC34D);   // amber
  static const purple = Color(0xFF8B5CF6);

  // Lines
  static const border = Color(0xFF1E335C);
  static const glow = Color(0x3322D3EE); // translucent cyan
}
